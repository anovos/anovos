name: Push to main

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'requirements.txt'

jobs:
  lint:
    uses: anovos/anovos/.github/workflows/lint.yml@main

  unit:
    needs:
      - lint
    uses: anovos/anovos/.github/workflows/unit.yml@main

  full_demo:
    needs:
      - unit
    uses: anovos/anovos/.github/workflows/full-demo.yml@main
